{{ $_ := env "CI_PROJECT_PATH_SLUG"  | trunc 49 | trimSuffix "-" | set . "ProjectName" }}
---
configVersion: 1
project: {{ .ProjectName }}
---
image: test-payments
from: python:alpine
git:
  - add: /
    to: /app
docker:
  WORKDIR: /app
shell:
  beforeInstall:
  - apk add chromium chromium-chromedriver xvfb
  - pip install pytest selenium allure-pytest requests webdriver-manager PyVirtualDisplay chromedriver
---
